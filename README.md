# MADE BY ❤ WITH NETHOST TEAM 

- CONTACT ME AT ```riveog_``` OR ```riveog__``` (DISCORD) FOR ANY ISSUE

# ⚙️ Installation

## 🚀 One-Line Installer (Recommended)

Install the panel instantly using our automated setup script:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/RIVEOG/hilos-host/main/install.sh)
```

---

## 📦 Description

This installer is designed to make setup as simple and universal as possible. It automatically configures everything required to run the panel without manual setup.

### 🌐 Supported Environments

This panel is built to run almost anywhere, including:

* 🖥️ **Any VPS (Ubuntu, Debian, etc.)**
* ☁️ **GitHub Codespaces**
* 💻 **GitHub IDX**
* 🧪 **CodeSandbox (Linux containers)**
* 🐳 **Docker-based environments (if enabled)**
* 🔧 Any cloud or local Linux environment with root access

---

## ⚡ What the Installer Does

When you run the command, it will automatically:

* 📥 Install all required dependencies (Node.js, packages, etc.)
* 🗄️ Configure database and backend services
* 🔐 Generate secure environment variables
* ⚙️ Set up system configuration automatically
* 🚀 Start all services and enable auto-run
* 🧹 Clean up temporary installation files

No manual configuration required — fully automated setup.

---

## 🛠️ Manual Installation (Optional)

```bash
git clone https://github.com/RIVEOG/hilos-host.git
cd HITLOS
bash install.sh
```

---

## ⚠️ Requirements

* Linux-based system (Ubuntu recommended)
* `bash` and `curl` installed
* Minimum 1GB RAM (2GB+ recommended for stability)
* Root or sudo access

---

## 💡 Note

If you're running this on cloud IDEs like Codespaces or IDX, make sure background services and ports are properly exposed.


